# PIPELINE — 週次AIトピックdigest生成手順

このドキュメントはクラウドroutine（Claude）向けの実行手順。
ここでやるのは **digestファイルの生成と main への push まで**。Slackへの配送は
GitHub Actions が行うため、webhook URL 等の認証情報を扱わない・書かない・出力しないこと。

## 1. トレンド収集

WebSearch で以下の観点を並列で検索する。いずれも**直近数日以内**の情報に限定
（クエリに期間や「today」「this week」を含める）:

- 生成AI・LLMの新モデル/新機能リリース
- AIエージェント・自動化ツールの話題
- 開発者向けAIツール（コーディング支援等）のアップデート
- 一般ユーザー向けAIサービスの話題・便利な使い方（非エンジニアでも試せるもの）
- AI業界の動向（投資・規制・企業導入・市場トレンド）
- 日本語圏で話題のAI記事・議論
- X（旧Twitter）発のバズ: 二次ソース経由で拾う
  （`allowed_domains: ["togetter.com", "posfie.com", "b.hatena.ne.jp"]` で `AI` `生成AI` を検索。
  可能なら元ポスト・一次発表URLを特定して添付）

## 2. トピック選定

3つの読者層をすべてカバーするように各1件・計3件選ぶ:

- 🔰 初学者向け: 普段使いのサービスの変化、非エンジニアが今日試せるもの。専門用語なしで説明できるネタ
- 🛠 開発者向け: モデルリリース、API・開発ツール、セキュリティ。具体的な数字があるもの
- 🏢 業界動向: 投資・規制・企業導入。「仕事の風向き」として読めるもの

共通基準:

- URLは検索結果で実在確認できたもののみ添付する。**URLを組み立てて捏造しない**
- 実行時点から1週間以上前のものは原則除外。重複・続報は1件にまとめる
- 憶測や未確認情報を断定調で書かない

## 3. digest整形と出力

`digest/YYYY-MM-DD.md`（実行日の日付、`date +%F`）に以下の形式で書く。
**ファイル内容がそのままSlackに投稿される**ため、Slack mrkdwn（`*太字*`・絵文字コード）で書き、
Markdownの見出し記号（`#`）は使わない:

```
:robot_face: いま話題のAIトピック（YYYY-MM-DD）
気になった話題はスレやスタンプで反応どうぞ :eyes:

:beginner: *<トピック名>*
<1〜2行の要約。平易に、何がうれしいかを書く>
→ <URL>

:hammer_and_wrench: *<トピック名>*
<要約。開発者向けは具体的に>
→ <URL>

:office: *<トピック名>*
<要約>
→ <URL>
```

- 各話題3〜5行、話題間は空行
- どれか1件だけに軽い問いかけを添えてよい

## 4. push

- 生成したファイルを **main ブランチに直接 commit & push** する（ブランチ・PRを作らない。
  main への push が配送Actionsのトリガー）
- コミットメッセージ例: `digest: 2026-07-27`
- 同日のファイルが既にある場合は上書きせず処理を中止する（二重投稿防止）

## 禁止事項

- 会社名・案件名・顧客情報に触れない（一般公開情報のトレンドのみ扱う）
- 業務利用を推奨する表現をしない。「個人学習・情報交換」のスコープを守る
- 特定ツールの導入を組織に迫るようなトーンにしない
- 認証情報（webhook URL等）をファイル・ログ・コミットに含めない
